{
  "name": "second-bolt-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "tsc -p .",
    "start": "node ./dist/index.js",
    "dev": "node --inspect -r ts-node/register ./src/index.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^2.3.0",
    "typescript": "^4.0.2"
  },
  "devDependencies": {
    "ts-node": "^9.0.0"
  }
}
